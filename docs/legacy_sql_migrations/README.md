# Legacy SQL Migrations

These files are archived for historical reference only. Alembic is the active
database migration system for FitPilot backend.

Do not add new SQL files here for runtime deployment. Create a new Alembic
revision instead and deploy it with `alembic upgrade head`.
