[pytest]
asyncio_mode = auto
# Session-scoped loop avoids "Event loop is closed" with asyncpg + Windows ProactorEventLoop
# when the SQLAlchemy engine pool reuses connections across tests.
asyncio_default_fixture_loop_scope = session
asyncio_default_test_loop_scope = session
testpaths = tests
